services:
  app:
    build:
      context: .
    environment:
      DATABASE_JDBC_URL: "${DATABASE_JDBC_URL:-jdbc:sqlite:./data/app.db}"
      PORT: "8080"
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - ./data:/opt/app/data
